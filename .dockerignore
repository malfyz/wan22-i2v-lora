# Keep source code — DO NOT ignore *.py or src/ !
# Trim only what bloats the build context.

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.venv/
venv/

# Git and local stuff
.git
.gitignore
.gitattributes

# Envs / secrets (ensure you don't commit real secrets)
.env
.env.*
*.env

# Build artifacts
dist/
build/
*.log

# Node
node_modules/

# OS junk
.DS_Store
Thumbs.db

# Datasets / big local caches (optional; remove if you need them inside the image)
data/
datasets/
outputs/
